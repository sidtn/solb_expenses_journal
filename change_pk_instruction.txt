На этом этапе удаляем FK у таблицы category а поле category делаем IntegerField
Миграция:
    migrations.AlterField(
        model_name='expense',
        name='category',
        field=models.IntegerField(),
    ),

SQL requests:

BEGIN;
CREATE TABLE "new__journal_api_expense" ("id" integer NOT NULL PRIMARY KEY AUTOINCREMENT, "category" integer NOT NULL, "amount" decimal NOT NULL, "short_description" varchar(255) NOT NULL, "created_at" datetime NOT NULL, "updated_at" datetime NOT NULL, "owner_id" bigint NOT NULL REFERENCES "journal_api_user" ("id") DEFERRABLE INITIALLY DEFERRED);
INSERT INTO "new__journal_api_expense" ("id", "amount", "short_description", "created_at", "updated_at", "owner_id", "category") SELECT "id", "amount", "short_description", "created_at", "updated_at", "owner_id", "category_id" FROM "journal_api_expense";
DROP TABLE "journal_api_expense";
ALTER TABLE "new__journal_api_expense" RENAME TO "journal_api_expense";
CREATE INDEX "journal_api_expense_owner_id_5914b08b" ON "journal_api_expense" ("owner_id");
COMMIT;





